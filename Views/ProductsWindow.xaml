<Window x:Class="CycleDesk.ProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CycleDesk.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="CycleDesk - Products" 
        Height="900" 
        Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#FFF5F5F5"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Action Button Style (Edit/Delete) -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Edit Button Style -->
        <Style x:Key="EditButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FF2D3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF1E2A36"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF141D28"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FFDC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFB21F2D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFADB5BD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="0"/>
                            <Grid x:Name="hoverLayer" Opacity="0">
                                <Border Width="3" HorizontalAlignment="Left" Background="White"/>
                                <Border Margin="3,0,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#33FFFFFF" Offset="0"/>
                                            <GradientStop Color="#03FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                            <Grid x:Name="activeLayer" Opacity="0">
                                <Border Width="3" HorizontalAlignment="Left" Background="White"/>
                                <Border Margin="3,0,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#33FFFFFF" Offset="0"/>
                                            <GradientStop Color="#03FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="activeLayer" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="{x:Null}"/>
                                </MultiTrigger.Conditions>
                                <MultiTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverLayer"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.EnterActions>
                                <MultiTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverLayer"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1" To="0" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.ExitActions>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFF8F9FA"/>
            <Setter Property="BorderBrush" Value="#FFDEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF2D3E50"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Focusable="False"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="{TemplateBinding BorderThickness}"
                                               CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Margin="15,0,0,0"
                                                                VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                <TextBlock Grid.Column="1" Text="▼" FontSize="10"
                                                         Foreground="#FF6C757D" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            Margin="15,0,30,0" VerticalAlignment="Center"
                                            HorizontalAlignment="Left" IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom"
                                  IsOpen="{TemplateBinding IsDropDownOpen}"
                                  AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="White" BorderBrush="#FFDEE2E6" BorderThickness="1"
                                       CornerRadius="8" Margin="0,5,0,0" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="200" Margin="0">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#FFF8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FFE9ECEF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <controls:SideMenuControl x:Name="sideMenu" DockPanel.Dock="Left" />

        <!-- GŁÓWNA ZAWARTOŚĆ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Górny bar -->
            <Border Grid.Row="0" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="0,0,0,1" Padding="30,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📦" FontSize="32" Margin="0,0,15,0"/>
                        <TextBlock Text="Products" FontSize="28" FontWeight="Bold"
                                  Foreground="#FF2D3E50" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtProductCount" Text="(0 products)" FontSize="14"
                                  Foreground="#FF6C757D" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </StackPanel>

                    <Button Content="➕ Add New Product" Height="45" Padding="25,0"
                           Background="#FF2D3E50" Foreground="White" BorderThickness="0"
                           FontWeight="SemiBold" FontSize="14" Cursor="Hand"
                           HorizontalAlignment="Right" VerticalAlignment="Center"
                           Click="OpenAddProductModal_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="10" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF1E2A36"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#FF141D28"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30">
                <Border Background="White" CornerRadius="15" Padding="30">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.1" Color="Black"/>
                    </Border.Effect>

                    <StackPanel>
                        <!-- Search & Filters -->
                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Search Box -->
                            <Border Grid.Column="0" Background="#FFF8F9FA" CornerRadius="10"
                                   Padding="15,12" BorderBrush="#FFDEE2E6" BorderThickness="1">
                                <Grid>
                                    <Viewbox Width="18" Height="18" HorizontalAlignment="Left"
                                            VerticalAlignment="Center" Margin="0,0,10,0">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                                                  Fill="#FF2D3E50"/>
                                        </Canvas>
                                    </Viewbox>
                                    <TextBox x:Name="txtSearch" Text="" FontSize="14" Foreground="#FF2D3E50"
                                            Background="Transparent" BorderThickness="0"
                                            VerticalAlignment="Center" Padding="35,0,0,0">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                                    <VisualBrush.Visual>
                                                                        <TextBlock Text="Search Product..." 
                                                                                  Foreground="#FF6C757D" 
                                                                                  FontSize="14" Margin="30,0,0,0"/>
                                                                    </VisualBrush.Visual>
                                                                </VisualBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </Border>

                            <!-- Filters -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                <ComboBox x:Name="cmbCategory" Width="150" Height="44" FontSize="13"
                                         Margin="0,0,10,0" Style="{StaticResource FilterComboBoxStyle}">
                                    <ComboBoxItem Content="All Categories" IsSelected="True"/>
                                </ComboBox>

                                <ComboBox x:Name="cmbStatus" Width="150" Height="44" FontSize="13"
                                          Margin="0,0,10,0" Style="{StaticResource FilterComboBoxStyle}">
                                    <ComboBoxItem Content="All Status" IsSelected="True"/>
                                    <ComboBoxItem Content="In Stock"/>
                                    <ComboBoxItem Content="Low Stock"/>
                                    <ComboBoxItem Content="Critical"/>
                                    <ComboBoxItem Content="Out of Stock"/>
                                </ComboBox>

                                <ComboBox x:Name="cmbSupplier" Width="150" Height="44" FontSize="13"
                                         Margin="0,0,10,0" Style="{StaticResource FilterComboBoxStyle}">
                                    <ComboBoxItem Content="All Suppliers" IsSelected="True"/>
                                </ComboBox>

                                <Button Width="44" Height="44" Background="#FF2D3E50" Foreground="White"
                                       BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"
                                       ToolTip="Refresh" Click="RefreshProducts_Click">
                                    <Button.Content>
                                        <Viewbox Width="20" Height="20">
                                            <Canvas Width="24" Height="24">
                                                <Path Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"
                                                      Fill="White"/>
                                            </Canvas>
                                        </Viewbox>
                                    </Button.Content>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="8">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF1E2A36"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#FF141D28"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="📥 Export CSV" Height="44" Padding="22,0"
                                       Background="#FF2D3E50" Foreground="White" BorderThickness="0"
                                       FontSize="13" FontWeight="SemiBold" Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF1E2A36"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#FF141D28"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <!-- Products Table -->
                        <Border BorderBrush="#FFDEE2E6" BorderThickness="1" CornerRadius="15">
                            <StackPanel>
                                <!-- Header -->
                                <Border Background="#FF2D3E50" CornerRadius="15,15,0,0" Height="50">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Image" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="1" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="SKU" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="2" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Product Name" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="3" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Category" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="4" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Stock" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="5" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Min" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="6" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Status" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="7" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Purchase" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="8" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Sale Price" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="9" Padding="15,0">
                                            <TextBlock Text="Supplier" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </Border>

                                <!-- Product Rows - DYNAMICZNIE GENEROWANE -->
                                <StackPanel x:Name="productsStackPanel">
                                    <!-- Wiersze będą dodawane z kodu C# -->
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <!-- FLOATING ACTION BUTTONS POPUP -->
            <Popup x:Name="actionButtonsPopup" Placement="MousePoint" StaysOpen="True"
                   AllowsTransparency="True" Closed="ActionButtonsPopup_Closed">
                <Border Background="White" CornerRadius="8" Padding="8"
                       BorderThickness="1" BorderBrush="#FFDEE2E6">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Edit" Width="60" Margin="0,0,8,0"
                               Style="{StaticResource EditButtonStyle}" Click="EditProduct_Click"/>
                        <Button Content="Delete" Width="70"
                               Style="{StaticResource DeleteButtonStyle}" Click="DeleteProduct_Click"/>
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>

        <!-- ADD PRODUCT MODAL OVERLAY -->
        <Grid x:Name="modalOverlay" Grid.ColumnSpan="2" Visibility="Collapsed"
              Background="#AA000000" Panel.ZIndex="1000">
            <Border Width="700" Height="650" Background="White" CornerRadius="15"
                   Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,30">
                        <TextBlock x:Name="txtModalTitle" Text="Add new product" FontSize="24"
                                  FontWeight="Bold" Foreground="#FF2D3E50"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Form Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Image Upload -->
                            <Border Width="120" Height="120" Background="#FFF8F9FA"
                                   BorderBrush="#FFDEE2E6" BorderThickness="2" CornerRadius="10"
                                   Margin="0,0,0,30" HorizontalAlignment="Left" Cursor="Hand">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Viewbox Width="40" Height="40" Margin="0,0,0,10">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z" Fill="#FF6C757D"/>
                                        </Canvas>
                                    </Viewbox>
                                    <TextBlock Text="[ Upload Image ]" FontSize="12"
                                              Foreground="#FF6C757D" TextAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Form Fields -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 1: Product Name | SKU -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Product Name:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="txtModalName" Height="40" Padding="12,0" FontSize="14"
                                            BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="SKU:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="txtModalSKU" Height="40" Padding="12,0" FontSize="14"
                                            BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 2: Category | Supplier -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Category:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="cmbModalCategory" Height="40" FontSize="14" Padding="12,0"
                                             BorderBrush="#FFDEE2E6" Style="{StaticResource FilterComboBoxStyle}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Supplier:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="cmbModalSupplier" Height="40" FontSize="14" Padding="12,0"
                                             BorderBrush="#FFDEE2E6" Style="{StaticResource FilterComboBoxStyle}"/>
                                </StackPanel>

                                <!-- Row 3: Purchase Price | Sale Price -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Purchase Price:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <Grid>
                                        <TextBox x:Name="txtModalPurchasePrice" Height="40" Padding="12,0,50,0"
                                                FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                            <TextBox.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </TextBox.Resources>
                                        </TextBox>
                                        <TextBlock Text="PLN" FontSize="13" Foreground="#FF6C757D"
                                                  VerticalAlignment="Center" HorizontalAlignment="Right"
                                                  Margin="0,0,15,0" IsHitTestVisible="False"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Sale Price:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <Grid>
                                        <TextBox x:Name="txtModalSellingPrice" Height="40" Padding="12,0,50,0"
                                                FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                            <TextBox.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </TextBox.Resources>
                                        </TextBox>
                                        <TextBlock Text="PLN" FontSize="13" Foreground="#FF6C757D"
                                                  VerticalAlignment="Center" HorizontalAlignment="Right"
                                                  Margin="0,0,15,0" IsHitTestVisible="False"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Row 4: Min Stock | Reorder Level -->
                                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Min Stock:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <Grid>
                                        <TextBox x:Name="txtModalMinStock" Height="40" Padding="12,0,50,0"
                                                FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1"
                                                Text="0" VerticalContentAlignment="Center">
                                            <TextBox.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </TextBox.Resources>
                                        </TextBox>
                                        <TextBlock Text="pcs." FontSize="13" Foreground="#FF6C757D"
                                                  VerticalAlignment="Center" HorizontalAlignment="Right"
                                                  Margin="0,0,15,0" IsHitTestVisible="False"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Reorder Level:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <Grid>
                                        <TextBox x:Name="txtModalReorderLevel" Height="40" Padding="12,0,50,0"
                                                FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1"
                                                Text="5" VerticalContentAlignment="Center">
                                            <TextBox.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </TextBox.Resources>
                                        </TextBox>
                                        <TextBlock Text="pcs." FontSize="13" Foreground="#FF6C757D"
                                                  VerticalAlignment="Center" HorizontalAlignment="Right"
                                                  Margin="0,0,15,0" IsHitTestVisible="False"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Row 5: Description -->
                                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3">
                                    <TextBlock Text="Description:" FontSize="13" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="txtModalDescription" Height="80" Padding="12" FontSize="14"
                                            BorderBrush="#FFDEE2E6" BorderThickness="1" TextWrapping="Wrap"
                                            AcceptsReturn="True" VerticalScrollBarVisibility="Auto">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer Buttons -->
                    <Grid Grid.Row="2" Margin="0,30,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="Cancel" Height="50" FontSize="15"
                               FontWeight="SemiBold" Background="#FFF8F9FA" Foreground="#FF6C757D"
                               BorderThickness="1" BorderBrush="#FFDEE2E6" Cursor="Hand"
                               Click="CloseModal_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                       BorderBrush="{TemplateBinding BorderBrush}"
                                                       BorderThickness="{TemplateBinding BorderThickness}"
                                                       CornerRadius="10">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FFE9ECEF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Grid.Column="2" Content="Save" Height="50" FontSize="15"
                               FontWeight="SemiBold" Background="#FF2D3E50" Foreground="White"
                               BorderThickness="0" Cursor="Hand" Click="SaveProduct_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FF1E2A36"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#FF141D28"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>