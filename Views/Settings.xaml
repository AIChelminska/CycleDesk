<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="CycleDesk.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CycleDesk.Controls"
        Title="CycleDesk - Settings"
        Height="900" 
        Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#FFF5F5F5"
        WindowState="Maximized">

    <Window.Resources>
        <!-- MENU BUTTON STYLE -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFADB5BD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="0"/>
                            <Grid x:Name="hoverLayer" Opacity="0">
                                <Border Width="3" HorizontalAlignment="Left" Background="White"/>
                                <Border Margin="3,0,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#33FFFFFF" Offset="0"/>
                                            <GradientStop Color="#03FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                            <Grid x:Name="activeLayer" Opacity="0">
                                <Border Width="3" HorizontalAlignment="Left" Background="White"/>
                                <Border Margin="3,0,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#33FFFFFF" Offset="0"/>
                                            <GradientStop Color="#03FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="activeLayer" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="{x:Null}"/>
                                </MultiTrigger.Conditions>
                                <MultiTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverLayer"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.EnterActions>
                                <MultiTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverLayer"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1" To="0" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.ExitActions>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- SUBMENU BUTTON STYLE -->
        <Style x:Key="SubmenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFADB5BD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="45,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#1AFFFFFF"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="Tag" Value="Active">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#26FFFFFF"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="25,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Action Button (Green) -->
        <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FF28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF218838"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF1E7E34"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Action Button -->
        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FFF8F9FA"/>
            <Setter Property="Foreground" Value="#FF6C757D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFDEE2E6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE9ECEF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

       
        <!-- Toggle Switch Style -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="switchBackground" 
                                   Width="50" Height="26" 
                                   CornerRadius="13" 
                                   Background="#FFDEE2E6"/>
                            <Border x:Name="switchThumb" 
                                   Width="20" Height="20" 
                                   CornerRadius="10" 
                                   Background="White"
                                   HorizontalAlignment="Left"
                                   Margin="3,0,0,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="switchBackground" Property="Background" Value="#FF28A745"/>
                                <Setter TargetName="switchThumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="switchThumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ComboBox Style -->
        <Style x:Key="SettingsComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFF8F9FA"/>
            <Setter Property="BorderBrush" Value="#FFDEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF2D3E50"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Focusable="False"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="{TemplateBinding BorderThickness}"
                                               CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                                Margin="15,0,0,0"
                                                                VerticalAlignment="Center"
                                                                HorizontalAlignment="Left"/>
                                                <TextBlock Grid.Column="1"
                                                         Text="▼"
                                                         FontSize="10"
                                                         Foreground="#FF6C757D"
                                                         Margin="0,0,15,0"
                                                         VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            Margin="15,0,30,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"
                                            IsHitTestVisible="False"/>

                            <Popup x:Name="PART_Popup"
                                  Placement="Bottom"
                                  IsOpen="{TemplateBinding IsDropDownOpen}"
                                  AllowsTransparency="True"
                                  Focusable="False"
                                  PopupAnimation="Slide">
                                <Border Background="White"
                                       BorderBrush="#FFDEE2E6"
                                       BorderThickness="1"
                                       CornerRadius="8"
                                       Margin="0,5,0,0"
                                       MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="200" Margin="0">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#FFF8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FFE9ECEF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="SettingsTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFF8F9FA"/>
            <Setter Property="BorderBrush" Value="#FFDEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF2D3E50"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                               Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="8"
                               SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="false"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"
                                        Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="#FFADB5BD"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="#FF28A745"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <controls:SideMenuControl x:Name="sideMenu" DockPanel.Dock="Left" />

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="40">
                <StackPanel MaxWidth="1200">
                    <!-- HEADER -->
                    <TextBlock Text="⚙️ Settings" FontSize="32" FontWeight="Bold" Foreground="#FF2D3E50" Margin="0,0,0,10"/>
                    <TextBlock Text="Configure application preferences and system settings" FontSize="14" Foreground="#FF6C757D" Margin="0,0,0,30"/>

                    <!-- GENERAL SETTINGS -->
                    <Border Background="White" CornerRadius="12" Padding="30" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.08"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🎨 General Settings" FontSize="20" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,25"/>

                            <!-- Language -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Language" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Select application display language" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ComboBox x:Name="cmbLanguage" Grid.Column="1" Width="200" Height="40" FontSize="14"
         Style="{StaticResource SettingsComboBoxStyle}"
         SelectedIndex="0" SelectionChanged="Language_SelectionChanged">
                                    <ComboBoxItem Content="🇵🇱 Polski"/>
                                    <ComboBoxItem Content="🇬🇧 English"/>
                                    <ComboBoxItem Content="🇩🇪 Deutsch"/>
                                </ComboBox>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Theme -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Theme" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Choose application color scheme" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ComboBox x:Name="cmbTheme" Grid.Column="1" Width="200" Height="40" FontSize="14"
         Style="{StaticResource SettingsComboBoxStyle}"
         SelectedIndex="0">
                                    <ComboBoxItem Content="☀️ Light"/>
                                    <ComboBoxItem Content="🌙 Dark"/>
                                    <ComboBoxItem Content="💻 System"/>
                                </ComboBox>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Currency -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Currency" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Default currency for prices and transactions" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ComboBox x:Name="cmbCurrency" Grid.Column="1" Width="200" Height="40" FontSize="14"
         Style="{StaticResource SettingsComboBoxStyle}"
         SelectedIndex="0">
                                    <ComboBoxItem Content="PLN - Polish Złoty"/>
                                    <ComboBoxItem Content="EUR - Euro"/>
                                    <ComboBoxItem Content="USD - US Dollar"/>
                                    <ComboBoxItem Content="GBP - British Pound"/>
                                </ComboBox>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Date Format -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Date Format" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Choose how dates are displayed" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ComboBox x:Name="cmbDateFormat" Grid.Column="1" Width="200" Height="40" FontSize="14"
         Style="{StaticResource SettingsComboBoxStyle}"
         SelectedIndex="0">
                                    <ComboBoxItem Content="DD.MM.YYYY"/>
                                    <ComboBoxItem Content="MM/DD/YYYY"/>
                                    <ComboBoxItem Content="YYYY-MM-DD"/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- SALES & POS SETTINGS -->
                    <Border Background="White" CornerRadius="12" Padding="30" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.08"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🛒 Sales &amp; POS Settings" FontSize="20" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,25"/>

                            <!-- Default Payment Method -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Default Payment Method" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Pre-select payment method in new sales" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <ComboBox x:Name="cmbDefaultPayment" Grid.Column="1" Width="200" Height="40" FontSize="14"
         Style="{StaticResource SettingsComboBoxStyle}"
         SelectedIndex="0">
                                    <ComboBoxItem Content="💵 Cash"/>
                                    <ComboBoxItem Content="💳 Card"/>
                                    <ComboBoxItem Content="None"/>
                                </ComboBox>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Show Product Images in POS -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Show Product Images in POS" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Display product images in sales interface" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <CheckBox x:Name="chkShowProductImages" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" IsChecked="True"/>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Enable Discounts -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Enable Discounts" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Allow applying discounts in sales" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <CheckBox x:Name="chkEnableDiscounts" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" IsChecked="True"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- DATABASE SETTINGS -->
                    <Border Background="White" CornerRadius="12" Padding="30" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.08"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="💾 Database Settings" FontSize="20" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,25"/>

                            <!-- Database Connection -->
                            <Grid Margin="0,0,0,20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                                    <TextBlock Text="Connection String" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Database connection configuration" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <TextBox x:Name="txtConnectionString" Grid.Row="1" Height="40" FontSize="14" 
        Style="{StaticResource SettingsTextBoxStyle}"
        Text="Server=localhost;Database=CycleDesk;Trusted_Connection=True;"/>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Backup Settings -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Automatic Backup" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Enable automatic database backups" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <CheckBox x:Name="chkAutoBackup" Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}" IsChecked="False"/>
                            </Grid>

                            <Border Height="1" Background="#FFDEE2E6" Margin="0,0,0,20"/>

                            <!-- Action Buttons -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Database Maintenance" FontSize="15" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                                    <TextBlock Text="Backup and restore operations" FontSize="13" Foreground="#FF6C757D" Margin="0,4,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="📤 Backup Now" Style="{StaticResource SecondaryActionButtonStyle}" 
                                       Margin="0,0,10,0" Click="BackupDatabase_Click"/>
                                <Button Grid.Column="2" Content="📥 Restore" Style="{StaticResource SecondaryActionButtonStyle}"
                                       Click="RestoreDatabase_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- SYSTEM INFORMATION -->
                    <Border Background="White" CornerRadius="12" Padding="30" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.08"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="ℹ️ System Information" FontSize="20" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,25"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Application Name:" FontSize="14" Foreground="#FF6C757D" Margin="0,0,0,15"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="CycleDesk - Bicycle Shop Management" FontSize="14" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,15"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Version:" FontSize="14" Foreground="#FF6C757D" Margin="0,0,0,15"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="1.0.0" FontSize="14" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,15"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="License:" FontSize="14" Foreground="#FF6C757D" Margin="0,0,0,15"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="Commercial License" FontSize="14" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,15"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Last Update:" FontSize="14" Foreground="#FF6C757D" Margin="0,0,0,15"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="November 2025" FontSize="14" FontWeight="SemiBold" Foreground="#FF2D3E50" Margin="0,0,0,15"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Developer:" FontSize="14" Foreground="#FF6C757D"/>
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="University Project" FontSize="14" FontWeight="SemiBold" Foreground="#FF2D3E50"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- ACTION BUTTONS -->
                    <Grid Margin="0,20,0,40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="1" Content="↩️ Reset to Defaults" 
                               Style="{StaticResource SecondaryActionButtonStyle}" 
                               Width="200" Margin="0,0,15,0"
                               Click="ResetToDefaults_Click"/>
                        <Button Grid.Column="2" Content="💾 Save Settings" 
                               Style="{StaticResource PrimaryActionButtonStyle}" 
                               Width="200"
                               Click="SaveSettings_Click"/>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
