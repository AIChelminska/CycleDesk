<Window x:Class="CycleDesk.SuppliersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:controls="clr-namespace:CycleDesk.Controls"
        Title="CycleDesk - Suppliers" 
        Height="900" 
        Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#FFF5F5F5"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Action Button Style (Edit/Delete) -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Edit Button Style -->
        <Style x:Key="EditButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FF2D3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF1E2A36"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF141D28"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FFDC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FFB21F2D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- MENU BUTTON STYLE -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFADB5BD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="0"/>
                            <Grid x:Name="hoverLayer" Opacity="0">
                                <Border Width="3" HorizontalAlignment="Left" Background="White"/>
                                <Border Margin="3,0,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#33FFFFFF" Offset="0"/>
                                            <GradientStop Color="#03FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                            <Grid x:Name="activeLayer" Opacity="0">
                                <Border Width="3" HorizontalAlignment="Left" Background="White"/>
                                <Border Margin="3,0,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#33FFFFFF" Offset="0"/>
                                            <GradientStop Color="#03FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="activeLayer" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="{x:Null}"/>
                                </MultiTrigger.Conditions>
                                <MultiTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverLayer"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1"
                                                         Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.EnterActions>
                                <MultiTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverLayer"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1" To="0"
                                                         Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.ExitActions>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFF8F9FA"/>
            <Setter Property="BorderBrush" Value="#FFDEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF2D3E50"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Focusable="False"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="{TemplateBinding BorderThickness}"
                                               CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Margin="15,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                <TextBlock Grid.Column="1" Text="▼" FontSize="10" Foreground="#FF6C757D" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            Margin="15,0,30,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"
                                            IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="White" BorderBrush="#FFDEE2E6" BorderThickness="1" CornerRadius="8" Margin="0,5,0,0" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="200" Margin="0">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#FFF8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FFE9ECEF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <controls:SideMenuControl x:Name="sideMenu" DockPanel.Dock="Left" />

        <!-- GŁÓWNA ZAWARTOŚĆ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Górny bar -->
            <Border Grid.Row="0" 
                    Background="White" 
                    BorderBrush="#FFE0E0E0" 
                    BorderThickness="0,0,0,1"
                    Padding="30,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🚚" FontSize="32" Margin="0,0,15,0"/>
                        <TextBlock Text="Suppliers"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#FF2D3E50"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="➕ Add New Supplier"
                            Height="45"
                            Padding="25,0"
                            Background="#FF2D3E50"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"
                            FontSize="14"
                            Cursor="Hand"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="OpenAddSupplierModal_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF1E2A36"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#FF141D28"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30">
                <Border Background="White" CornerRadius="15" Padding="30">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.1" Color="Black"/>
                    </Border.Effect>

                    <StackPanel>
                        <!-- Search & Filters -->
                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="450"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Search Box -->
                            <Border Grid.Column="0" Background="#FFF8F9FA" CornerRadius="10" Padding="15,12" BorderBrush="#FFDEE2E6" BorderThickness="1">
                                <Grid>
                                    <Viewbox Width="18" Height="18" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z" Fill="#FF2D3E50"/>
                                        </Canvas>
                                    </Viewbox>
                                    <TextBox x:Name="txtSearch" Text="" FontSize="14" Foreground="#FF2D3E50" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Padding="35,0,0,0"
                                             GotFocus="SearchTextBox_GotFocus" LostFocus="SearchTextBox_LostFocus" TextChanged="SearchTextBox_TextChanged">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                                    <VisualBrush.Visual>
                                                                        <TextBlock Text="Search Supplier..." Foreground="#FF6C757D" FontSize="14" Margin="30,0,0,0"/>
                                                                    </VisualBrush.Visual>
                                                                </VisualBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </Border>

                            <!-- Filters -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                <ComboBox x:Name="cmbStatus" Width="150" Height="44" FontSize="13" Margin="0,0,10,0" Style="{StaticResource FilterComboBoxStyle}" SelectionChanged="cmbStatus_SelectionChanged">
                                    <ComboBoxItem Content="All Status" IsSelected="True"/>
                                    <ComboBoxItem Content="Active"/>
                                    <ComboBoxItem Content="Inactive"/>
                                </ComboBox>

                                <ComboBox x:Name="cmbCountry" Width="150" Height="44" FontSize="13" Margin="0,0,10,0" Style="{StaticResource FilterComboBoxStyle}" SelectionChanged="cmbCountry_SelectionChanged">
                                    <ComboBoxItem Content="All Countries" IsSelected="True"/>
                                </ComboBox>

                                <!-- Refresh Button -->
                                <Button Width="44" Height="44" Background="#FF2D3E50" Foreground="White" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" ToolTip="Refresh" Click="RefreshButton_Click">
                                    <Button.Content>
                                        <Viewbox Width="20" Height="20">
                                            <Canvas Width="24" Height="24">
                                                <Path Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" Fill="White"/>
                                            </Canvas>
                                        </Viewbox>
                                    </Button.Content>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF1E2A36"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Export CSV Button -->
                                <Button Content="📥 Export CSV" Height="44" Padding="22,0" Background="#FF2D3E50" Foreground="White" BorderThickness="0" FontSize="13" FontWeight="SemiBold" Cursor="Hand" Click="ExportCSV_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF1E2A36"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <!-- Suppliers Table -->
                        <Border BorderBrush="#FFDEE2E6" BorderThickness="1" CornerRadius="15">
                            <StackPanel>
                                <!-- Header -->
                                <Border Background="#FF2D3E50" CornerRadius="15,15,0,0" Height="50">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="150"/>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="140"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="ID" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="1" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Supplier Name" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="2" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Contact Person" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="3" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Phone" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="4" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Email" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="5" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="City" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="6" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Country" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="7" BorderBrush="#FF4A5568" BorderThickness="0,0,1,0" Padding="15,0">
                                            <TextBlock Text="Status" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="8" Padding="15,0">
                                            <TextBlock Text="Actions" FontSize="12" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" TextAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </Border>

                                <!-- Dynamic Rows -->
                                <ItemsControl x:Name="icSuppliers">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#FFDEE2E6" BorderThickness="0,1,0,0" Background="White">
                                                <Grid Height="60">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="150"/>
                                                        <ColumnDefinition Width="140"/>
                                                        <ColumnDefinition Width="200"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="140"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding SupplierCode}" FontSize="13" Foreground="#FF2D3E50" VerticalAlignment="Center" TextAlignment="Center" FontWeight="SemiBold"/>
                                                    </Border>
                                                    <Border Grid.Column="1" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding SupplierName}" FontSize="13" Foreground="#FF2D3E50" VerticalAlignment="Center" FontWeight="Medium" TextTrimming="CharacterEllipsis"/>
                                                    </Border>
                                                    <Border Grid.Column="2" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding ContactPerson}" FontSize="13" Foreground="#FF6C757D" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                    </Border>
                                                    <Border Grid.Column="3" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding Phone}" FontSize="13" Foreground="#FF6C757D" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border Grid.Column="4" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding Email}" FontSize="13" Foreground="#FF6C757D" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                    </Border>
                                                    <Border Grid.Column="5" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding City}" FontSize="13" Foreground="#FF6C757D" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border Grid.Column="6" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="15,0">
                                                        <TextBlock Text="{Binding Country}" FontSize="13" Foreground="#FF6C757D" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border Grid.Column="7" BorderBrush="#FFDEE2E6" BorderThickness="0,0,1,0" Padding="10,0">
                                                        <Border Background="{Binding StatusBackground}" CornerRadius="6" Padding="8,4" HorizontalAlignment="Center" VerticalAlignment="Center" Height="28">
                                                            <TextBlock Text="{Binding StatusText}" FontSize="11" Foreground="{Binding StatusColor}" FontWeight="SemiBold"/>
                                                        </Border>
                                                    </Border>
                                                    <Border Grid.Column="8" Padding="10,0">
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                            <Button Content="Edit" Width="50" Height="28" Margin="0,0,5,0" Style="{StaticResource EditButtonStyle}" Click="EditSupplier_Click"/>
                                                            <Button Content="Delete" Width="60" Height="28" Style="{StaticResource DeleteButtonStyle}" Click="DeleteSupplier_Click"/>
                                                        </StackPanel>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Footer -->
                        <TextBlock x:Name="txtTotalCount" Text="Showing 0 suppliers" Foreground="#FF6C757D" FontSize="13" Margin="0,15,0,0"/>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>

        <!-- ADD SUPPLIER MODAL OVERLAY -->
        <Grid x:Name="modalOverlay" Grid.ColumnSpan="2" Visibility="Collapsed" Background="#AA000000" Panel.ZIndex="1000">
            <Border Width="700" Height="700" Background="White" CornerRadius="15" Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,30">
                        <TextBlock x:Name="modalTitle" Text="Add new supplier" FontSize="24" FontWeight="Bold" Foreground="#FF2D3E50" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Button Content="✕" Width="32" Height="32" HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" Foreground="#FF6C757D" BorderThickness="0" FontSize="20" Cursor="Hand" Click="CloseModal_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FFF8F9FA"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <!-- Form Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 0: Supplier Name | Contact Person -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Supplier Name: *" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtSupplierName" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Contact Person: *" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtContactPerson" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 1: Phone | Email -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Phone: *" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtPhone" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Email: *" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtEmail" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 2: Address -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
                                    <TextBlock Text="Street Address:" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtAddress" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 3: City | Postal Code -->
                                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="City:" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtCity" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Postal Code:" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtPostalCode" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <!-- Row 4: Tax ID | Country -->
                                <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,20">
                                    <TextBlock Text="Tax ID (NIP):" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <TextBox x:Name="modalTxtTaxID" Height="40" Padding="12,0" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" VerticalContentAlignment="Center">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>

                                <StackPanel Grid.Row="4" Grid.Column="2" Margin="0,0,0,20">
                                    <TextBlock Text="Country:" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="modalCmbCountry" Height="40" FontSize="14" Padding="12,0" BorderBrush="#FFDEE2E6" Style="{StaticResource FilterComboBoxStyle}">
                                        <ComboBoxItem Content="Poland" IsSelected="True"/>
                                        <ComboBoxItem Content="Germany"/>
                                        <ComboBoxItem Content="Netherlands"/>
                                        <ComboBoxItem Content="Czech Republic"/>
                                        <ComboBoxItem Content="Taiwan"/>
                                        <ComboBoxItem Content="Other"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Row 5: Status -->
                                <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
                                    <TextBlock Text="Status:" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                    <ComboBox x:Name="modalCmbStatus" Height="40" FontSize="14" Padding="12,0" BorderBrush="#FFDEE2E6" Style="{StaticResource FilterComboBoxStyle}">
                                        <ComboBoxItem Content="Active" IsSelected="True"/>
                                        <ComboBoxItem Content="Inactive"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <!-- Notes -->
                            <StackPanel Margin="0,0,0,0">
                                <TextBlock Text="Notes / Additional Information:" FontSize="13" FontWeight="SemiBold" Foreground="#FF6C757D" Margin="0,0,0,8"/>
                                <TextBox x:Name="modalTxtNotes" Height="80" Padding="12,10" FontSize="14" BorderBrush="#FFDEE2E6" BorderThickness="1" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto">
                                    <TextBox.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </TextBox.Resources>
                                </TextBox>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer Buttons -->
                    <Grid Grid.Row="2" Margin="0,30,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="Cancel" Height="50" FontSize="15" FontWeight="SemiBold" Background="#FFF8F9FA" Foreground="#FF6C757D" BorderThickness="1" BorderBrush="#FFDEE2E6" Cursor="Hand" Click="CancelModal_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FFE9ECEF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button x:Name="btnSaveSupplier" Grid.Column="2" Content="Save Supplier" Height="50" FontSize="15" FontWeight="SemiBold" Background="#FF2D3E50" Foreground="White" BorderThickness="0" Cursor="Hand" Click="SaveSupplier_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FF1E2A36"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>